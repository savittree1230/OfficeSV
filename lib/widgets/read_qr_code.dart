import 'package:flutter/material.dart';

class ReadQrCode extends StatelessWidget {
  const ReadQrCode({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('This is ReadQrCode'),
    );
  }
}
